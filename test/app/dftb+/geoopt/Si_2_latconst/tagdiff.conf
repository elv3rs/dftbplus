# Lattice constant optimization on a very flat PES near equilibrium.
# Different convergence paths (due to IO layer changes) lead to coordinates
# that differ by ~1.4e-5 while energy agrees to ~1e-10. Relax end_coords tolerance.
end_coords:         @ 2.0e-5
